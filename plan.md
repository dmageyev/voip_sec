# План лекції 7: Безпека IP-АТС на прикладі Asterisk

**Курс:** Технології VoIP  
**Аудиторія:** Студенти магістратури, напрям «Кібербезпека»  
**Тривалість:** 2 академічні години (90 хвилин)  
**Форма:** Лекція з демонстраціями та практичними прикладами конфігурацій

---

## 1. Навчальні цілі

Після лекції студенти зможуть:

1. Пояснити архітектуру захищеної IP-АТС та роль принципу мінімізації привілеїв.
2. Налаштовувати ACL на рівні IP у конфігурації Asterisk та на мережевому рівні (iptables/nftables, fail2ban).
3. Використовувати контексти `chan_sip` / `chan_pjsip` для ізоляції груп користувачів у диалплані.
4. Виявляти та усувати типові вразливості диалплану (Toll Fraud, DISA, некоректна валідація введення).
5. Застосовувати комплекс захисних заходів: TLS/SRTP, Digest Auth, захист AMI, аудит конфігурацій.

---

## 2. Місце лекції в курсі

| Лекція | Тема |
|--------|------|
| 1 | Технології VoIP: загальний огляд, SIP, RTP |
| 2 | Протокол H.323 |
| 3–6 | Медіа-протоколи, SRTP, ZRTP |
| **7** | **Безпека IP-АТС на прикладі Asterisk** |
| 8+ | Безпека WebRTC, практичні лабораторні роботи |

Лекція спирається на знання SIP (Лекція 1), SRTP (Лекції 3–6) та розширює їх до рівня адміністрування та захисту реальної системи.

---

## 3. Детальний план (структура за слайдами)

### Блок 1. Вступ та огляд (Слайди 1–4) — ~10 хв

**Слайд 1. Титульний**
- Назва, курс, кафедра.

**Слайд 2. План лекції**
- Перелік усіх блоків, ключові терміни.

**Слайд 3. IP-АТС у сучасній інфраструктурі**
- Різниця між IP-АТС і традиційною PSTN АТС.
- Чому безпека IP-АТС — критична задача.
- Статистика: Toll Fraud становить мільярди доларів збитків на рік.
- Відкритий vs комерційний програмний стек (Asterisk, FreeSWITCH, FreeRADIUS).

**Слайд 4. Архітектура Asterisk**
- Компоненти: channel drivers (chan_pjsip, chan_sip, chan_dahdi), core PBX engine, dialplan, modules.
- Місце Asterisk у мережевій схемі: UA ↔ Asterisk ↔ SIP Trunk / PSTN GW.
- Поверхня атаки: SIP-стек, AMI, AGI, HTTP, файлова система, ОС.

---

### Блок 2. Принцип мінімізації привілеїв (Слайди 5–8) — ~15 хв

**Слайд 5. Principle of Least Privilege (PoLP)**
- Визначення і чому це фундаментальний принцип.
- Як PoLP застосовується до IP-АТС: ОС, конфігурація, мережа, диалплан.

**Слайд 6. Захист на рівні операційної системи**
- Запуск Asterisk від непривілейованого користувача (`asterisk:asterisk`).
- Параметри `-U` / `-G` при старті демона.
- Права доступу до `/etc/asterisk/`, `/var/lib/asterisk/`, `/var/log/asterisk/`.
- `systemd` unit-файл: `NoNewPrivileges`, `ProtectSystem`, `PrivateTmp`.

**Слайд 7. Захист конфігураційних файлів**
- Відокремлення секретів: паролі SIP, AMI credentials.
- `#include` для зберігання секретів у окремих файлах з жорсткими правами.
- Шифрування диску / Vault для sensitive даних.
- Версійний контроль конфігурацій (Git + pre-commit hooks для виявлення секретів).

**Слайд 8. Мережева ізоляція АТС**
- Розміщення Asterisk у виділеному VLAN.
- Схема: LAN Users VLAN ↔ PBX VLAN ↔ SIP Trunk VLAN.
- Принцип: кожен компонент має доступ тільки до необхідних ресурсів.

---

### Блок 3. ACL — Контроль доступу на рівні IP (Слайди 9–13) — ~15 хв

**Слайд 9. ACL: концепція та рівні**
- Рівень 1: Мережевий файрвол (iptables / nftables / pfSense).
- Рівень 2: Asterisk внутрішні ACL (`permit`/`deny` у конфігах).
- Рівень 3: fail2ban / Snort — динамічний захист.
- Defense-in-depth: жоден рівень не є достатнім сам по собі.

**Слайд 10. ACL у chan_sip (sip.conf)**
- Директиви `permit`, `deny`, `acl`.
- Приклади конфігурації: дозвіл тільки корпоративної підмережі.
- `contactpermit`, `contactdeny` — перевірка Contact-header у REGISTER.
- `allowsubscribe`, `allowoverlap` — зменшення поверхні атаки.

**Слайд 11. ACL у chan_pjsip (pjsip.conf)**
- Об'єкт `[acl]` типу `acl`.
- Прив'язка ACL до endpoint через `acl=` і `contact_acl=`.
- Порівняння підходів chan_sip і chan_pjsip.

**Слайд 12. ACL для AMI та HTTP**
- `manager.conf`: `permit`, `deny` для AMI-підключень.
- `http.conf`: обмеження доступу до embedded HTTP-сервера.
- Принцип: AMI відкритий тільки для localhost / моніторинг-хостів.

**Слайд 13. fail2ban + iptables/nftables**
- Як fail2ban читає лог Asterisk і блокує IP-адреси.
- Конфігурація `asterisk.conf` jail та filter.
- `nftables` правила для обмеження SIP-реєстрацій (rate limiting).
- Команда: `nft add rule inet filter input ip saddr ...`.

---

### Блок 4. Контексти та ізоляція груп користувачів (Слайди 14–17) — ~15 хв

**Слайд 14. Контексти у Asterisk: що це?**
- Контекст як простір імен для extension'ів у диалплані.
- Зв'язок: SIP endpoint → context → доступні extension.
- Схема: внутрішні користувачі, зовнішній trunk, IVR — у різних контекстах.

**Слайд 15. Ізоляція груп у chan_sip**
- Параметр `context=` в `[general]`, `[peer]`, `[user]`.
- Приклад: `context=internal` для IP-телефонів, `context=from-trunk` для SIP-провайдера.
- Чому небезпечно використовувати один контекст для всіх.

**Слайд 16. Ізоляція груп у chan_pjsip**
- `context=` в endpoint-секції.
- Конфігурація trunk endpoint vs user endpoint.
- `identify` об'єкти для автентифікації по IP без пароля (і ризики).

**Слайд 17. Ієрархія контекстів: include**
- Директива `include => ` в extensions.conf.
- Дерево включень: від обмеженого до розширеного доступу.
- Типова помилка: `include => from-internal` у контексті trunk.
- Best practice: явний дозвіл, заборона за замовчуванням.

---

### Блок 5. Безпека диалплану (Слайди 18–24) — ~20 хв

**Слайд 18. Загрози на рівні диалплану**
- Toll Fraud: несанкціоновані дзвінки на міжнародні / преміум номери.
- DISA (Direct Inward System Access) — відкритий доступ до ліній.
- Voicemail → outbound: вихід у зовнішню мережу через VM-меню.
- Вразливості AGI/FastAGI-скриптів.

**Слайд 19. Toll Fraud: механізм і масштаб**
- Як зловмисник отримує доступ до SIP-акаунта.
- Схема: brute force → реєстрація → дзвінок на дорогі номери → збиток.
- Реальні цифри: збитки телекомпаній та підприємств.
- Типові цілі: +900, міжнародні, преміум-напрямки.

**Слайд 20. Захист від Toll Fraud у диалплані**
- Whitelist дозволених напрямків у `extensions.conf`.
- `CALLERID(num)` / `CHANNEL(peerip)` — перевірка джерела.
- Заборона `_+.`, `_011.`, `_1900NXXXXXX` паттернів для зовнішніх контекстів.
- `AMPUSER` / `AMAFLAGS` для CDR-аудиту.

**Слайд 21. Валідація введення в диалплані**
- Небезпека довільного введення: `GotoIf`, `Exec`, `System()`.
- `SanitizeInput` через `FILTER()` функцію.
- Регулярні вирази у Extension паттернах: дозволяти тільки цифри.
- Обережно з `AGI()`, `System()`, `SHELL()` — можливе виконання команд.

**Слайд 22. Вразливості DISA**
- Що таке DISA і навіщо використовується.
- Чому DISA без PIN — критична вразливість.
- Захист: складний PIN, обмеження часу, whitelist номерів, логування.
- Рекомендація: уникати DISA або замінити на VPN + внутрішній дзвінок.

**Слайд 23. Захист Voicemail та IVR**
- Типова вразливість: вихід у external через VM-опцію «передзвони».
- Заборона `OutgoingCallExten` або обмеження контекстом.
- IVR: обмеження кількості спроб введення, таймаути, логування.
- DTMF injection в IVR: валідація довжини та формату введення.

**Слайд 24. Ліміти та антифрод-механізми**
- `GROUP()` / `GROUP_COUNT()` — обмеження одночасних викликів на акаунт.
- `REALTIME` / CDR: виявлення аномальних паттернів.
- Зовнішні антифрод-системи: FraudBuster, ASTPP, власні AGI-скрипти.
- Алерти: SMS/email при перевищенні порогу витрат.

---

### Блок 6. Додаткові механізми захисту (Слайди 25–28) — ~10 хв

**Слайд 25. SIP Digest Authentication**
- Механізм challenge-response у Asterisk.
- `secret=` у peer-секції: вимоги до паролів (довжина, складність).
- `alwaysauthreject=yes` — захист від enumeration.
- `maxexpirey`, `minexpirey` — обмеження терміну реєстрацій.

**Слайд 26. TLS та SRTP в Asterisk**
- Конфігурація TLS для SIP у `sip.conf` / `pjsip.conf`.
- Генерація сертифікатів: Let's Encrypt або внутрішній CA.
- `encryption=yes` у PJSIP endpoint для SRTP.
- `media_encryption_optimistic` vs `required`.

**Слайд 27. Захист AMI (Asterisk Manager Interface)**
- AMI — потужний інтерфейс, небезпечний при неправильному налаштуванні.
- `manager.conf`: мінімальні права (`read`, `write`), обмеження по IP.
- Уникати plain-text; розглянути ARI (Asterisk REST Interface) з HTTPS.
- Принцип: кожна система моніторингу — окремий AMI-акаунт з мінімальними правами.

**Слайд 28. Логування, аудит і моніторинг**
- `logger.conf`: рівні `VERBOSE`, `SECURITY`, `WARNING`.
- SECURITY-events: реєстрація невдалих аутентифікацій.
- CDR (Call Detail Records) та CEL (Channel Event Logging).
- Інтеграція з SIEM (ELK Stack, Splunk, Graylog).
- Команди: `asterisk -rx "core show channels"`, `sngrep`, Wireshark.

---

### Блок 7. Підсумок і кращі практики (Слайди 29–31) — ~5 хв

**Слайд 29. Типові помилки конфігурації**
- Таблиця: помилка → наслідок → виправлення.
- Default пароль AMI / SIP.
- `allowguest=yes` у sip.conf.
- Один контекст для всіх.
- Відсутність fail2ban.
- `System()` з некерованим введенням.

**Слайд 30. Чек-ліст безпеки Asterisk**
- ОС та файлова система.
- Мережевий захист (файрвол, VLAN).
- ACL у конфігурації Asterisk.
- Контексти та ізоляція.
- Диалплан: валідація, whitelist.
- Автентифікація та шифрування.
- Моніторинг та аудит.

**Слайд 31. Q&A та ресурси**
- Ключові RFC та документація.
- Питання студентів.

---

## 4. Методика проведення

| Час | Форма роботи |
|-----|-------------|
| 0–10 хв | Вступ, огляд Asterisk, актуалізація знань |
| 10–25 хв | Лекція + демонстрація: PoLP, захист ОС |
| 25–40 хв | Лекція + приклади конфігурацій: ACL |
| 40–55 хв | Лекція + аналіз схем: контексти |
| 55–75 хв | Лекція + розбір кейсів: безпека диалплану |
| 75–85 хв | Огляд додаткових механізмів |
| 85–90 хв | Підсумок, чек-ліст, Q&A |

### Методичні прийоми
- **Live demo:** показ конфігурацій у реальному Asterisk або Docker-контейнері.
- **Кейс-стаді:** реальний кейс Toll Fraud — аналіз логів, пошук вразливості.
- **Pitfall-секції:** що не треба робити та чому.
- **Checklists:** студенти заповнюють чек-ліст для умовної компанії.

---

## 5. Матеріали для студентів

### Рекомендована документація
- [Asterisk Security Best Practices](https://wiki.asterisk.org/wiki/display/AST/Asterisk+Security+Best+Practices) — офіційна вікі
- [VoIP Security Alliance (VOIPSA)](http://www.voipsa.org/) — спільнота та ресурси
- RFC 3261 (SIP), RFC 3711 (SRTP), RFC 5746 (TLS renegotiation)

### Інструменти
| Інструмент | Призначення |
|------------|-------------|
| `sngrep` | Аналіз SIP-трафіку в терміналі |
| Wireshark | Глибокий аналіз пакетів |
| `fail2ban` | Динамічне блокування атак |
| `sipvicious` (svmap, svcrack) | Тестування на проникнення SIP |
| `sipp` | Генерація навантаження / тест |
| Asterisk `core set debug` | Детальне логування |

### Практичне завдання (до наступної лекції)
> Розгорніть Asterisk у Docker, налаштуйте три контексти (internal, from-trunk, restricted), додайте ACL для кожного, реалізуйте захист диалплану від Toll Fraud. Зробіть скріншоти конфігурацій та продемонструйте спробу обійти захист.

---

## 6. Ключові терміни

| Термін | Визначення |
|--------|-----------|
| **ACL** | Access Control List — список правил permit/deny для IP-адрес |
| **Toll Fraud** | Несанкціоноване використання телефонних ліній для дорогих дзвінків |
| **Контекст** | Простір імен у диалплані Asterisk, що ізолює групи extension |
| **PoLP** | Principle of Least Privilege — мінімум необхідних прав |
| **DISA** | Direct Inward System Access — вхід у ATC ззовні з відкритим доступом до ліній |
| **AMI** | Asterisk Manager Interface — API для управління та моніторингу |
| **chan_pjsip** | Сучасний SIP-драйвер Asterisk на базі бібліотеки PJSIP |
| **chan_sip** | Застарілий (legacy) SIP-драйвер Asterisk |
| **CDR** | Call Detail Record — запис про дзвінок |
| **CEL** | Channel Event Logging — детальний журнал подій каналу |

---

## 7. Оцінювання знань

### Контрольні запитання (для самоперевірки)
1. Що таке принцип мінімізації привілеїв і як він застосовується до конфігурації Asterisk на рівні ОС?
2. Яка різниця між `permit`/`deny` у `sip.conf` і правилами iptables?
3. Чому небезпечно мати один контекст для внутрішніх користувачів і зовнішнього SIP-транку?
4. Опишіть сценарій атаки Toll Fraud крок за кроком.
5. Які Extension-паттерни слід заборонити для запобігання несанкціонованим дзвінкам?
6. Що таке DISA і чому вона є вразливістю без належного захисту?
7. Як fail2ban захищає Asterisk від брутфорс-атак?
8. Яка різниця між `media_encryption_optimistic` та обов'язковим SRTP?

### Критерії оцінки практичного завдання
| Критерій | Балів |
|----------|-------|
| Коректне налаштування 3 контекстів | 20 |
| Робочі ACL (permit/deny) | 20 |
| Захист диалплану від Toll Fraud | 25 |
| Налаштування fail2ban | 15 |
| Документація та коментарі | 10 |
| Демонстрація спроби обходу | 10 |
| **Разом** | **100** |
